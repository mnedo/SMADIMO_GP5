# SMADIMO_GP5

## Структура
```text
Repo/
├── README.md                 # описание проекта
├── .gitignore
├── data/
│   ├── raw/                  # изначальные датасеты
│   └── processed/            # датасеты после обработки
└── models/
    ├── tabular/              # часть 1 — табличные данные
    │   ├── model_*.pt
    │   └── logs/
    └── image/                # часть 2 — неструктурированные данные
        ├── model_*.pt
        └── logs/
```


> Mikhail:
Timeline 
----------------------------------------
0. [Миша, дд 07.06] Организация репозитория
Dir: Данные, Модели, Ноутбуки + .gitgonre + README старт

1. [Ксюша, дд 08.06] Репарсинг Oskelly с Claw4AI
Сбор данных с Oskelly для последующих задач. Используем доп. инструмент как требуют в гп - это легче даже для нас. Делаем через Crawl4AI - софт сам все подтянет с Oskelly после настройки. С сайта нужны картинки и все описание товара
----------------------------------------
2. [Савелий, дд 09.06] Обработка датасетов (не скучно)
Главные задачи: превратить текстовое описание в осмысленные признаки. Нужно выбрать мультиязыковой векторайзер и применить его к описанию вещей в наш Oskelly. Oskelly идет как на предсказание цены, так и для изображений. В задаче просят базовый EDA: 15% - использованные средства предобработки, подготовки данных и EDA.

+ Надо обработать датасет Second-Hand Fashion Dataset. Но оттуда нам нужно только изображение + качество. Минимум обработки, но нужно чтобы распределение данных по возможности было сопоставимо с Oskelly датасетом 
----------------------------------------
3. [Ксюша, дд 13.06] Простейшая MLP
Неглубокая MLP (до 2-3 слоев), не нужны Dropout, LayerNorm и т.п. Смотрим что будет, если применить базовую нейросеть. Однако надо будет искать параметры архитектуры перебором: количество нейронов, функции активации, варианты optimizer. Все логируем и обоснование каждого архитектурного решения в readme.

4. [Савелий, дд 13.06] Глубокая MLP
Полноценная MLP - много слоев, dropout и layernorm - все, что было использовано на курсе, но не больше. Надо будет искать параметры архитектуры перебором: количество нейронов, слоев, функции активации, варианты нормировка, доля dropout, варианты optimizer. Все логируем и обоснование каждого архитектурного решения в readme.

5. [Катя, дд 13.06] MLP нестандартной архитектуры
MLP за рамками курса. "MLP с нестандартной архитектурой". Можно точно сделать residual connection - чтобы не затухали градиенты. Можно попробовать сделать несколько голов (то есть не связывать все нейроны между уровнями и сделать как бы n каналов обработки сигналов). Немного творчества, больше свобод - надо будет искать параметры архитектуры перебором, все логируем - все в readme

**Часть 2: DL на неструктурированных данных**
**Базовый CNN** - сверточная сеть, обучаем с нуля. Состоит из 3х сверточных слоев (Conv, ReLU, MaxPool) и 3х полносвязных слоев - классификаторов. Количество классов(=выходов) 13. Перед тем, как загонять фотографии в классификатор, они были приведены к размеру 256x256 и нормализованы. Разделение на train и test = 80% на 20% (сохраняется напротяжении всей части 2). Параметры обучения: Adam, lr=3e-4, batch=256, 10 эпох

Было протестировано 4 конфигурации:

| Параметр | base | bn_dout | bn_dout2 | bn_dout3 |
|---|---|---|---|---|
| Каналы conv | 3-16-32-64 | 3-16-32-64 | 3-16-32-48 | 3-16-32-48 |
| BatchNorm | нет | да | да | да |
| FC | 256-64-13 | 256-64-13 | 128-32-13 | 128-32-13 |
| Активация FC | Tanh | Tanh | ReLU | ReLU |
| Dropout | нет | 0.3 / 0.25 | 0.5 / 0.5 | 0.3 / 0.3 |
| macro-F1 | 0.550 | 0.494 | 0.586 | 0.529 |

Лучшая конфигурация — bn_dout2 (macro-F1 = 0.586).
----------------------------------------
# Transfer Learning - EfficientNet-B3

## Задача

Классификация изображений товаров с маркетплейса Oskelly на 13 категорий с использованием Transfer Learning на базе предобученной сверточной сети EfficientNet-B3.

## Данные

- Датасет: `oskelly_dataset.xlsx` - 7 717 товаров после фильтрации
- Разбивка: 80% train (6 173) / 20% val (1 544), используем stratify
- 13 классов: Блузы и рубашки, Брюки, Верхняя одежда, Джинсы, Жакеты и пиджаки, Жилеты, Майки и футболки, Платья, Топы с длинным рукавом, Трикотаж, Худи и толстовки, Шорты, Юбки

Мелкие категории (менее 1% от датасета) были исключены через фильтр `top_labels`, так как при таком количестве сэмплов смодель не может выучить редкие классы надежно.

## Выбор архитектуры

Выбрана **EfficientNet-B3** по следующим причинам:

- Использует compound scaling - одновременное масштабирование глубины, ширины и разрешения сети, что даёт лучшее соотношение качество/размер модели по сравнению с ResNet
- Одна из лучших известных моделей; на ImageNet выдает показатели лучше, чем, например, ResNet
- Среди всех EfficientNet-B{X} не слишком слабая и не слишком мощная (для приемлевого срока обучения)
- Архитектура не пересекается с CLIP-ViT (трансформер), который используется в соседнем эксперименте

Стандартные mean/std нормализации соответствуют ImageNet: `[0.485, 0.456, 0.406]` / `[0.229, 0.224, 0.225]`. ([Отсюда взяяли значения](https://docs.pytorch.org/vision/stable/models/generated/torchvision.models.efficientnet_b3.html))

## Аугментации

Для train применены лёгкие аугментации:

```python
transforms.RandomHorizontalFlip()
transforms.RandomRotation(15)
transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2)
```

Агрессивные аугментации не использовались, так как у нас относительно небольшой датасет. Сильные трансформации могут значительно ухудшить качество. Val-трансформы включают только Resize и Normalize.

## Стратегия обучения: трехэтапный Transfer Learning

Выбрана трехэтапная стратегия постепенной разморозки backbone. При полной заморозке backbone модель не может адаптировать низкоуровневые признаки под специфику наших данных, а при полной разморозке сразу имеется риск переобучения на сравнительно малом датасете. Поэтапная разморозка позволяет сначала обучить голову, а затем потихоньку дообучить backbone.

### Stage 1 - Feature Extraction (frozen backbone)

- Заморожены все слои backbone, обучается только классификатор
- Классификатор: `Dropout(0.3) → Linear(1536, 13)`
- Optimizer: Adam, lr=1e-3
- Epochs: 10
- Логирование: [Comet ML - EfficientNetB3 stage1](https://www.comet.com/gp5-team1/oskelly-cv/3202da114b8741d797e2ae4efc07294e)

На последней эпохе (десятой) было достигнуто значение 0.7024 для метрики f1.

**Вывод:** backbone хорошо извлекает признаки одежды даже без дообучения - val_f1 вырос с 0.54 до 0.70 за 10 эпох только за счет обучения классификатора.

### Stage 2 - Fine-tuning (разморозка features.7–8)

- Разморожены последние два блока backbone (`features.7`, `features.8`) + классификатор
- Optimizer: Adam, lr=1e-4 (в 10 раз меньше, чтобы не разрушить предобученные веса)
- Epochs: 5
- Логирование: [Comet ML - EfficientNetB3 stage2](https://www.comet.com/gp5-team1/oskelly-cv/20f157c0e6be4bac8398a89d8489f9fe)

На четвертой эпохе (из пяти) было достигнуто значение 0.7672 для метрики f1.

**Вывод:** разморозка последних блоков дала существенный прирост: val_f1 поднялся с 0.70 до почти 0.77. Блоки `features.7–8` отвечают за высокоуровневые признаки (силуэт, форма), которые наиболее релевантны для fashion-классификации.

### Stage 3 - Deep Fine-tuning (разморозка features.5–8)

- Разморожены блоки `features.5–8` + классификатор
- Optimizer: Adam, lr=1e-5 (еще в 10 раз меньше)
- Epochs: 5
- Логирование: [Comet ML - EfficientNetB3 stage3](https://www.comet.com/gp5-team1/oskelly-cv/3330f2fb5e9f499886b6e0e60d31ed45)

На четвертой эпохе (из пяти) было достигнуто значение 0.7679 для метрики f1.

**Вывод:** дальнейшая разморозка дала минимальный прирост (менее 0.001 f1), а на последней эпохе val_loss резко вырос - признак начала переобучения. Значит, обучение было остановлено своевременно.

## Итоговые результаты

Лучшая модель (Stage 2, epoch 4): **val_acc = 0.8199, val_macro_f1 = 0.7679**

### Classification Report

```
                        precision    recall  f1-score   support

       Блузы и рубашки       0.73      0.60      0.66        80
                 Брюки       0.88      0.92      0.90        73
        Верхняя одежда       0.68      0.91      0.78       206
                Джинсы       0.94      0.90      0.92        98
      Жакеты и пиджаки       0.70      0.53      0.60        40
                Жилеты       0.68      0.54      0.60        24
      Майки и футболки       0.91      0.93      0.92       369
                Платья       0.88      0.77      0.82       164
Топы с длинным рукавом       0.56      0.43      0.49        42
              Трикотаж       0.74      0.74      0.74       152
      Худи и толстовки       0.87      0.76      0.81       160
                 Шорты       0.86      0.95      0.90        76
                  Юбки       0.91      0.80      0.85        60

              accuracy                           0.82      1544
             macro avg       0.79      0.75      0.77      1544
          weighted avg       0.82      0.82      0.82      1544
```

### Анализ результатов по классам

**Лучшие классы (f1 > 0.85):** Джинсы (0.92), Майки и футболки (0.92), Шорты (0.90), Брюки (0.90) - визуально четкие и хорошо представленные классы.

**Слабые классы (f1 < 0.65):** Топы с длинным рукавом (0.49), Блузы и рубашки (0.66), Жакеты и пиджаки (0.60), Жилеты (0.60).

Основные причины ошибок:
- **Топы с длинным рукавом** путаются с трикотажем, худи и толстовками - схожий силуэт, отличие в основном только в материале, которое сложно различить визуально
- **Блузы и рубашки** путаются с верхней одеждой и майками - граница между категориями субъективна, силуэты также схожи
- **Жилеты** - малочисленные классы (40 и 24 примера в val), модель видела их меньше

Низкое качество на редких классах является следствием дисбаланса датасета (Майки и футболки: 369 vs Жилеты: 24), а не ограничением архитектуры.

## Итоговое сравнение этапов

Наибольший прирост качества дал Stage 2. Stage 3 улучшил результат незначительно при заметном росте риска переобучения, что подтверждает правильность остановки на Stage 2 как финальной модели.

----------------------------------------
**CLIP** — это модель OpenAI, которую обучали на сотнях миллионов пар (фото + подпись) методом контрастивного обучения: энкодер картинок и энкодер текста притягивались, если пара правильная, и отталкивались, если нет. В итоге image encoder научился выдавать эмбеддинги, которые реально отражают семантику изображения — не просто текстуры и края, как у CNN, а что-то вроде «понимания» что на фото.
Мы этот энкодер замораживаем (не трогаем) и просто вешаем сверху маленький классификатор-голову, который учим уже на наших метках состояния. То есть мы не обучаем 400M параметров — мы обучаем ~1-2 слоя поверх готовых хороших признаков.


Протестировано 5 конфигураций головы (все — frozen CLIP encoder, обучается только голова):
Runtest macro-F1
CLIPHEAD big0.8231
CLIPHEAD base_d050.8172
CLIPHEAD small0.8159
CLIPHEAD base_d000.8089
CLIPHEAD BASE0.7954

Лучшая: CLIPHEAD big — macro-F1 = 0.8231 на эпохе ~96.
Ключевые наблюдения для сравнительного вывода (Item 10):

Сходимость очень быстрая (~20 эпох до плато) — энкодер уже несёт сильный сигнал.
Dropout 0.5 (base_d05) почти не уступает большой голове, маленький MLP (small) тоже конкурентен — переобучения нет даже без регуляризации.
Baseline без дополнительных слоёв (CLIPHEAD BASE) заметно слабее — голова имеет значение.

----------------------------------------
9. [Ксюша, дд 14.06] Оформление README табличных данных
Оформляем summary по первой части ГП на основе содержания от ребят. Цель в критерии: "Кроме того, в рамках сравнительного анализа должны быть получены и сформулированы явные, значимые выводы о сравнительной эффективности той или иной архитектуры. В рамках сравнения должны быть получены и сформулированы явные, значимые выводы о том, как изменение архитектуры влияет на качество решения задачи". Текстовое резюме работы + сами как раз разберетесь что остальные сделали
